.lang-switcher.dropdown
  button.btn.dropdown-toggle(type="button", data-toggle="dropdown")
    | Language
  .dropdown-menu
    each item in langSwitcher()
      a.dropdown-item(href!=item.path)
        != item.name
